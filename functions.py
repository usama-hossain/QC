# functions from programming drills exercises

import numpy as np

def isSquare(A):
    rows = len(A)
    for row in A:
        if len(row) != rows
            return False
    return True

def isHermitian(A):
    if isSquare(A) == False:
        return False